query Download($locale: SiteLocale) {
  download(locale: $locale) {
    slug
    title
    seoMeta {
      title
      description
      image {
        url
      }
    }
    downloads {
      body
      file {
        url
        filename
        format
      }
    }
  }
}
