---
layout: base
pagination:
  data: allPages
  size: 1
  alias: item
permalink: /{{ item.locale }}/{{ item.slug }}/
eleventyComputed:
  locale: "{{ item.locale }}"
---
{# {% from "modular-content/index.njk" import modularContent %} #}

<main>
  {{ item.title }}
  {{ item.body[1].decisionTree.branches | log }}
  {# {{ modularContent(item.body, { data: { allArticles: allArticles } }) }} #}
</main>
