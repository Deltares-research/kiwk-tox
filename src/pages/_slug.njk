---
layout: base
pagination:
  data: allPages
  size: 1
  alias: item
permalink: /{{ item.locale }}/{{ item.slug }}/index.html
---
{# {% from "modular-content/index.njk" import modularContent %} #}

<main>
  {{ item.title }}
  {# {{ modularContent(item.body, { data: { allArticles: allArticles } }) }} #}
</main>
